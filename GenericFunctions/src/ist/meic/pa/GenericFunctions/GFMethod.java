package ist.meic.pa.GenericFunctions;

public class GFMethod {
	/**
	 * This class is empty because each created instance will do its own
	 * implementation, as expected when using anonymous inner classes. This
	 * enables us to not have a "default" call method and let the user code its
	 * implementation to their taste.
	 */
}
